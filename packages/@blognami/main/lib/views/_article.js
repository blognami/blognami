
export const styles = ({ colors }) => `

`;

export default {
    render(){
        const { meta, title, body, footer } = this.params;

        return this.renderHtml`
            
        `;
    }
}