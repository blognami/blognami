// Rows that include('sitemappable') form the `this.database.sitemappables` union; Union.create applies
// each member table's `usedAsSitemappable` scope when that table declares one.
export default {
    meta(){}
};
