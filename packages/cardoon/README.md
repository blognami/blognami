# Cardoon

Cardoon is an agent-orchestration framework built on haberdash. It composes `AbstractCommand` and `AbstractServiceFactory` from haberdash to provide a standalone CLI, proving the haberdash composition pattern works independently of Pinstripe.
